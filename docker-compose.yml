version: '3'
services:
  web:
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - "8088:3000" # First port is exposed port. Don't change second port unless you know what you're doing
